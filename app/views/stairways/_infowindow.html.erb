<% if user %>
  <%= form_for stairway.user_runs.new, :url => user_runs_path do |f| %>
    <%= f.hidden_field :stairway_id, value: stairway.id %>
    <%= f.hidden_field :user_id, value: user.id %>
    <%= f.submit "Add run" %>
  <% end %>
<% end %>
<%= stairway.description.html_safe %>
